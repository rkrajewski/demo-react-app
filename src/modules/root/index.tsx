import React from 'react'

import { Box } from '@mui/material'

function Root() {
  return (
    <Box
      sx={{
        padding: 1,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'flex-start',
      }}
    >
      Hello world
    </Box>
  )
}

export default Root
